"use strict";

const helper = require("../../../helper.js");
const lib = helper.requireModule('./mw/mt/lib');
const assert = require('assert');


describe("Unit test for: mw - mt lib", () => {

    it("Add test here", (done) => {
        done();
    });
});